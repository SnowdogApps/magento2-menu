<?php /** @var \Snowdog\Menu\Block\Menu $block */ ?>
<?php
    $menuClass = $block->getMenu()->getCssClass();
    $node = $block->getMenuNode();
    $nodeId = $node->getNodeId();
    $classes = $menuClass;
    $classes .= $node->getLevel() == 0 ? '__link' : '__inner-link';
    $classes .= $node->getClasses() ? ' ' . $node->getClasses() : '';
    $dataAttribute = $nodeId ? ' data-menu="menu-' . $nodeId . '"' : '';
    $itemLinkUrl = $block->getNodeTypeProvider($node->getType())->getCategoryUrl($nodeId);
?>
<a href="<?= $itemLinkUrl ?>" class="<?= $classes ?>"<?= $dataAttribute ?>><?= $node->getTitle() ?></a>
